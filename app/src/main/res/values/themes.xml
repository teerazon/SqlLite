<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ProductManagement" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ProductManagement" parent="Base.Theme.ProductManagement" />
    <!-- เพิ่ม styles สำหรับปุ่ม -->
    <style name="Widget.AppCompat.Button.Colored" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/bg_button_primary</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:drawablePadding">8dp</item>
    </style>

    <style name="Widget.AppCompat.Button.Borderless"
        parent="Widget.AppCompat.Button.Borderless"
        tools:ignore="ResourceCycle">
        <item name="android:textColor">@color/primary</item>
        <item name="android:drawablePadding">8dp</item>
    </style>
</resources>